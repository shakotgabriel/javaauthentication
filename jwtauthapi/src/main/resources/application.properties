spring.application.name=jwtauthapi

# Default local profile uses in-memory H2 to avoid requiring external DB credentials.
spring.profiles.default=h2

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
